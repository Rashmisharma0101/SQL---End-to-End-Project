# Project Overview
This project is an end-to-end SQL case study where I created a database, loaded raw data,
built SQL queries
The goal is to demonstrate strong SQL skills including joins, aggregations, window functions, CTEs
